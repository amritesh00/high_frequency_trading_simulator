services:
  - type: web
    name: hft-simulator
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app/main.py"
    envVars:
      - key: API_KEY
        value: demo123
